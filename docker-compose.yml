version: '3.8'

services:
  mysql-db:
    image: mysql:8.0
    container_name: fifa_mysql_container
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: fifa_world_cup
      MYSQL_USER: challenger_user
      MYSQL_PASSWORD: challengerpassword
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: